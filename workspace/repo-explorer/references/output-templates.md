# 中文输出模板

## 项目概览报告模板

```markdown
# [项目名] 项目分析报告

## 📋 基本信息

| 属性 | 值 |
|------|-----|
| **项目名称** | [name] |
| **项目类型** | [Node.js / Python / Rust / Go / Java] |
| **主要语言** | [TypeScript / Python / Rust / Go] |
| **许可证** | [MIT / Apache 2.0 / GPL / 其他] |
| **仓库地址** | [URL] |
| **Star 数** | [从 GitHub 获取] |

## 📝 项目简介

[从 README 提取的项目描述，1-3 段]

**核心功能：**
- 功能点 1
- 功能点 2
- 功能点 3

## 🛠 技术栈

### 运行环境
- **运行时**: [Node.js 18+ / Python 3.10+ / ...]
- **包管理**: [npm / pnpm / poetry / cargo / ...]

### 核心依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| `express` | 4.18.x | Web 框架 |
| `prisma` | 5.x | ORM |
| `zod` | 3.x | 数据验证 |

### 开发工具

| 工具 | 用途 |
|------|------|
| TypeScript | 类型检查 |
| ESLint | 代码规范 |
| Jest | 单元测试 |

## 📁 目录结构

```
[project-name]/
├── src/
│   ├── index.ts          # 应用入口
│   ├── app.ts            # 应用配置
│   ├── routes/           # 路由定义
│   │   ├── index.ts
│   │   └── users.ts
│   ├── services/         # 业务逻辑
│   │   └── user.service.ts
│   ├── models/           # 数据模型
│   │   └── user.model.ts
│   └── utils/            # 工具函数
├── tests/                # 测试文件
├── docs/                 # 文档
├── package.json
└── tsconfig.json
```

## 🏗 架构概览

### 架构风格

[描述项目采用的架构风格]

### 架构图

```
┌─────────────────────────────────────────────────────┐
│                    Presentation                      │
│  ┌─────────────────────────────────────────────┐    │
│  │              Routes / Controllers             │    │
│  └─────────────────────┬───────────────────────┘    │
├────────────────────────┼────────────────────────────┤
│                    Application                       │
│  ┌─────────────────────▼───────────────────────┐    │
│  │                  Services                     │    │
│  └─────────────────────┬───────────────────────┘    │
├────────────────────────┼────────────────────────────┤
│                      Domain                          │
│  ┌─────────────────────▼───────────────────────┐    │
│  │           Models / Entities                   │    │
│  └─────────────────────┬───────────────────────┘    │
├────────────────────────┼────────────────────────────┤
│                   Infrastructure                     │
│  ┌─────────────────────▼───────────────────────┐    │
│  │         Database / External APIs              │    │
│  └───────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

## 🔑 核心模块

### [模块名 1]

- **位置**: `src/core/[module]/`
- **职责**: [模块功能描述]
- **关键文件**:
  - `index.ts` - 模块入口
  - `service.ts` - 核心逻辑
- **关键类/函数**:
  - `ClassName` - 类的作用
  - `functionName()` - 函数的作用

### [模块名 2]

...

## 🚀 入口点分析

应用启动流程：

1. **入口文件**: `src/index.ts:1`
   - 加载环境变量
   - 初始化日志

2. **应用初始化**: `src/app.ts:15`
   - 配置中间件
   - 注册路由

3. **启动服务**: `src/index.ts:25`
   - 监听端口
   - 输出启动日志

## 📖 推荐阅读顺序

### 第一阶段：理解项目结构（预计 30 分钟）

1. `README.md` - 了解项目目标
2. `package.json` - 了解依赖和脚本
3. `src/index.ts` - 了解启动流程

### 第二阶段：理解核心机制（预计 1-2 小时）

4. `src/app.ts` - 应用配置
5. `src/routes/` - 路由结构
6. `src/services/` - 业务逻辑

### 第三阶段：深入特定功能

根据需要选择：
- 认证机制: `src/auth/`
- 数据库操作: `src/models/`
- API 集成: `src/integrations/`

## 🔧 使用指南

### 安装

```bash
# 克隆仓库
git clone [repo-url]
cd [project-name]

# 安装依赖
npm install

# 配置环境变量
cp .env.example .env
```

### 开发

```bash
# 启动开发服务器
npm run dev

# 运行测试
npm test

# 构建生产版本
npm run build
```

### 常用配置

| 环境变量 | 说明 | 默认值 |
|----------|------|--------|
| `PORT` | 服务端口 | 3000 |
| `DATABASE_URL` | 数据库连接 | - |
| `LOG_LEVEL` | 日志级别 | info |

## ❓ 常见问题

### Q: [问题 1]？

A: [解答]

### Q: [问题 2]？

A: [解答]
```

## 函数/类解析模板

```markdown
## 函数解析: `[functionName]`

**位置**: `src/path/to/file.ts:42`

### 签名

```typescript
function functionName(
    param1: Type1,
    param2: Type2
): ReturnType
```

### 功能说明

[用中文解释函数的作用，2-3 句话]

### 参数说明

| 参数 | 类型 | 说明 |
|------|------|------|
| `param1` | `Type1` | 参数作用 |
| `param2` | `Type2` | 参数作用 |

### 返回值

- **类型**: `ReturnType`
- **说明**: 返回值含义

### 调用示例

```typescript
const result = functionName(arg1, arg2);
```

### 内部逻辑

1. 首先，检查参数有效性
2. 然后，执行核心逻辑
3. 最后，返回处理结果

### 调用关系

**被以下函数调用：**
- `callerFunction1()` at `src/other.ts:100`
- `callerFunction2()` at `src/another.ts:50`

**调用以下函数：**
- `helperFunction()` at `src/utils.ts:20`

### 相关代码

- [相关类型定义](src/types.ts:15)
- [相关工具函数](src/utils.ts:20)
```

## 类解析模板

```markdown
## 类解析: `[ClassName]`

**位置**: `src/path/to/file.ts:10`

### 类图

```
┌─────────────────────────────┐
│        ClassName            │
├─────────────────────────────┤
│ - privateField: Type        │
│ + publicField: Type         │
├─────────────────────────────┤
│ + constructor(params)       │
│ + publicMethod(): Type      │
│ - privateMethod(): void     │
└─────────────────────────────┘
          △
          │ extends
┌─────────────────────────────┐
│       BaseClass             │
└─────────────────────────────┘
```

### 职责

[用中文描述类的职责，1-2 段]

### 属性

| 属性 | 类型 | 可见性 | 说明 |
|------|------|--------|------|
| `field1` | `Type1` | public | 属性说明 |
| `field2` | `Type2` | private | 属性说明 |

### 方法

#### `methodName(params): ReturnType`

[方法说明]

### 使用示例

```typescript
const instance = new ClassName(config);
instance.doSomething();
```

### 继承关系

- **继承自**: `BaseClass`
- **实现接口**: `Interface1`, `Interface2`
- **子类**: `SubClass1`, `SubClass2`

### 设计模式

[如果类使用了特定设计模式，在此说明]
```

## 错误/边界情况模板

```markdown
## ⚠️ 注意事项

### 边界情况

1. **空输入处理**
   - 当参数为空时，函数会 [行为说明]
   - 建议：[使用建议]

2. **并发访问**
   - 此函数 [是/否] 线程安全
   - 原因：[解释]

### 常见错误

| 错误类型 | 触发条件 | 解决方法 |
|----------|----------|----------|
| `TypeError` | 参数类型错误 | 检查参数类型 |
| `NetworkError` | 网络连接失败 | 重试或检查网络 |

### 性能考虑

- 时间复杂度: O(n)
- 空间复杂度: O(1)
- 注意：大数据量时可能需要 [优化建议]
```

## 探索进度模板

```markdown
## 📊 探索进度

### 已分析

- [x] 项目概览
- [x] 核心架构
- [x] 入口点分析

### 进行中

- [ ] 中间件系统
- [ ] 数据库层

### 待探索

- [ ] 认证机制
- [ ] 错误处理
- [ ] 测试覆盖

---

**需要我继续分析哪个部分？**

1. 中间件系统的实现细节
2. 数据库访问层设计
3. 认证和授权机制
4. 其他：[请说明]
```
